<h1>Listing users</h1>

<table>
  <tr>
    <th>Meno</th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.name %></td>
    <td><%= link_to 'Zobrazit', user %></td>
    <td><%= link_to 'Upravit', edit_user_path(user) %></td>
    <td><%= link_to 'Odstranit', user, method: :delete, data: { confirm: 'Naozaj odstranit?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Novy uzivatel', new_user_path %>
